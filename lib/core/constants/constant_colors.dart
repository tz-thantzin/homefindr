import 'package:flutter/material.dart';

// ─────────────────────────────────────────────
// Luxury palette — Sotheby's / editorial dark style
// ─────────────────────────────────────────────

/// Gold accent — all CTAs, badges, active states, icons
const kPrimary = Color(0xFFC9A84C);

/// Lighter gold for hover / shimmer highlights
const kGoldLight = Color(0xFFE8C97A);

/// Deeper gold for pressed / focus rings
const kGoldDark = Color(0xFFA8893D);

/// Gold glow layer (10% opacity, for card hover shadow tint)
const kGoldGlow = Color(0x1AC9A84C);

/// Main app canvas — deepest dark
const kSecondary = Color(0xFF0F0F0F);

/// Footer background — deeper than kSecondary
const kFooterBg = Color(0xFF0A0A0A);

/// Navbar scrolled background
const kNavBg = Color(0xFF0D0D0D);

/// Dark section background (alternating with kSecondary)
const kCanvas = Color(0xFF141414);

/// Card / dialog surface
const kSurface = Color(0xFF1C1C1C);

/// Hovered card surface
const kSurfaceHover = Color(0xFF242424);

/// Warm cream — primary text on dark backgrounds
const kCream = Color(0xFFF5F0E8);

/// Muted text — secondary / descriptive copy
const kMuted = Color(0xFF9B9B9B);

/// Subtle border on dark surfaces
const kBorderDark = Color(0xFF2A2A2A);

/// Dividers
const kDividerDark = Color(0xFF252525);

/// Status badge — For Rent (deep navy-teal)
const kBadgeRent = Color(0xFF1E4D6A);

/// Status badge — Sold (slate)
const kBadgeSold = Color(0xFF374151);

// ─────────────────────────────────────────────
// Semantic / utility
// ─────────────────────────────────────────────
const kBlack = Color(0xFF000000);
const kWhite = Color(0xFFFFFFFF);
const kBlue = Color(0xFF2152FF);
const kRed = Color(0xFFFF0000);
const kRed100 = Color(0xFFFFEBEE);
const kTransparent = Colors.transparent;
const kBlack12 = Colors.black12;

// ─────────────────────────────────────────────
// Grey scale (kept for shimmer gradients & legacy refs)
// ─────────────────────────────────────────────
const Color kGrey25 = Color(0xFFFCFCFC);
const Color kGrey100 = Color(0xFFF2F2F2);
const Color kGrey200 = Color(0xFFE0E0E0);
const Color kGrey300 = Color(0xFFCECECE);
const Color kGrey400 = Color(0xFFBCBCBC);
const Color kGrey500 = Color(0xFFABABAB);
const Color kGrey700 = Color(0xFF878787);
const Color kGrey800 = Color(0xFF6B6B6B);
const Color kGrey900 = Color(0xFF5E5E5E);
const Color kGrey1000 = Color(0xFF515151);
const Color kGrey1200 = Color(0xFF2E2E2E);

// ─────────────────────────────────────────────
// Dark shimmer gradient stops
// ─────────────────────────────────────────────
const Color kDarkShimmer1 = Color(0xFF1A1A1A);
const Color kDarkShimmer2 = Color(0xFF242424);
